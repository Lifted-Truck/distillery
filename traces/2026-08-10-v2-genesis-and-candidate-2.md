# v2-genesis-and-candidate-2 — library-entry.2 adopted; genesis regenerated; candidate #2 designated

- **Queue item:** ROADMAP decisions 15–17; provider ruling response-002
  (ball was ours: parser to v2, fixtures moved, clears report).
- **Why:** The provider ruled all five distillery-002 questions
  (`library-entry.2`); the human directed proceeding. Two critic rounds
  shaped the parser design (REWORK verdict with a corpus-prototyped review:
  phantom-span guard, byte-exact pipe restoration, terminators, repeated
  labels, exhaustive still-quarantine list, genesis-regeneration instead of
  a seen-key amendment). Lead integration then found+fixed one more real
  regression the critic's corpus check missed: attest's back-to-back
  pipe-bearing entries (span-open now also triggers on a pipe-bearing
  marker line; regression tests added, 82 total).
- **Evidence consulted:** response-002.md; kit/contracts/library-entry.md
  (v2); critic review (REWORK, 6 blockers + prototype over 34 LIBRARYs);
  implementer trace traces/2026-08-10-library-entry-v2-parser.md (3 flags,
  all endorsed); real-corpus parses of attest/morphos/wont/HYPERSAW/Tonality.
- **Genesis regeneration (decision 16):** old 87-record v1 journal archived
  to session scratchpad (uncommitted, reproducible); stream regenerated from
  empty under v2: **126 lessons + 25 quarantines** across 62 registry
  projects. Integrity: all 68 v1 lessons preserved; clears vs v1 =
  **14 cleared / 2 correctly still-quarantined (Place#L0010 tier:retracted,
  vertex#L0001 supersedes:none) / 2 orphaned by the Tonality-Live rename** —
  exactly the critic's prediction, including the provider's 11→12 corpus
  drift + 2 unpredicted clears (refraction-bench#L0002, Place#L0011).
- **Candidate #2 (decision 17):** morphos#L0003 + HYPERSAW#L0020 —
  first observed two-origin recurrence; human-attested (D2 signatures
  cannot see it; D3's job). The pool entry merges hash-identity and
  timestamp-freshness as variants.
- **Verify:** `./verify full` exit 0 — 82 tests + replay_check OK.
  Re-stamped post-commit.
- **Open questions:** mailbox obligations filed with the report
  (report-002): bare-tier letter-vs-contract discrepancy; repeated-label
  rule (we continuation-join); structural/terminator + span-open grammar
  extensions as a v2 erratum; distillery-003 heading-style entries gap
  (20+ entries, incl. 3 in Tonality found at implementation). D3 remains
  the open phase.
