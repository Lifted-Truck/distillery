# d2-close — D2 sealed: queries + deterministic staleness (archive deferred)

Supersedes the open question in [2026-07-13-d2-query-cli.md](2026-07-13-d2-query-cli.md);
records the lead resolution + D2 gate result.

- **Queue item:** ROADMAP D2 (Stream operations). Gate: golden query results
  (lessons / recurrences exact+near / staleness) over a fixture stream.
- **Why:** D2 ships read-side value now — provenance queries + deterministic
  staleness — while the speculative archive/compaction machinery is deferred
  (decision 11) after two critic rounds proved it introduces a D1-dedup seam
  at a scale we are ~180× from needing. Nothing in D2 touches an invariant.
- **Resolution of the query-cli open question (lead):** "near" recurrence is
  ruled **case + whitespace insensitive ONLY; punctuation stays significant**
  (decision 9, now explicit in docs/stream-ops.md §1). The implementer built
  `near_signature` exactly to spec and correctly refused to invent
  punctuation-stripping — a grounded refusal (success class). The mismatch
  was in the *fixture*, not the code: its near-pair (alpha#L0006 / gamma#L0006)
  differed by a trailing period, so it (correctly) didn't group. Fixed the
  fixture so the pair differs by case + whitespace only (both end with a
  period); regenerated the fixture stream and all four goldens from the CLI;
  flipped the one documentation test to assert the pair now near-groups AND
  that exact-mode does not — so the golden demonstrates `--near` finding a
  group `--exact` misses. Extending the normalization instead was rejected:
  punctuation/stemming/synonymy is meaning-matching, which is D3's job.
- **Evidence consulted:** docs/stream-ops.md (revised spec), the two D2 critic
  reviews (REJECT → REJECT; the second caught the load-bearing §0 seam),
  distillery/query.py, tests/unit/test_query.py, tests/fixtures/stream/,
  tests/golden/q-*.json, ROADMAP decisions 9/10/11.
- **Alternatives rejected:** (1) build archive now with union-scan or a key
  index — deferred as speculative + seam-introducing (decision 11);
  (2) extend near normalization to strip punctuation — rejected as D2/D3
  boundary creep; (3) leave the fixture non-grouping and rely only on
  synthetic tests — rejected so a committed golden demonstrates near end-to-end.
- **Verify:** `./verify full` exit 0 — 59 tests (D1+D2) + replay_check OK.
  Golden query gate rides the unit suite (test_query.py subprocesses bin/query
  and byte-matches tests/golden/), so it runs under both fast and full; no
  charter-gated `verify` edit was needed. Recorded hash re-stamped post-commit.
- **Open questions:** none blocking. Archive/compaction remains deferred with
  its revival trigger + mandatory dedup-conservation fix recorded (decision 11
  / Deferred-demoted). distillery-002 (multi-line entries) still provider-ball,
  respond-by 2026-07-18 — unrelated to D2.
