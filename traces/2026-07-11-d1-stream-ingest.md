# d1-stream-ingest — D1: deterministic append-only stream ingest, gate passed

- **Queue item:** ROADMAP D1 (the stream — deterministic ingest). Gate:
  two consecutive real-tree sweeps (second a no-op), injected duplicate
  detected, ledger replays byte-identical, `./verify full` green.
- **Why:** Built the warehouse's ingest path: a fence-aware entry parser for
  `library-entry.1`, an append-only jsonl journal with (project, hash) dedup
  and journal-before-ledger crash ordering, a deterministic ingest core
  reusing the pinned sweep primitive, and a CLI adapter that reads the wall
  clock only at the edge (`--date`). Schema `stream-record.1` was
  critic-reviewed before implementation; all four blockers folded in
  (file-level provenance, fence-aware detection, write ordering, run-summary
  observability) — see ROADMAP decisions 5–8.
- **Evidence consulted:** docs/stream-schema.md (ratified schema);
  autonomous/kit/contracts/library-entry.md (`library-entry.1`);
  autonomous/kit/sweep/sweep.py (pinned primitive, reused not reimplemented);
  ROADMAP.md D1 gate + decisions 3–8; critic review (APPROVE-WITH-CHANGES);
  distillery/entry_parser.py, journal.py, ingest.py, bin/ingest.
- **Gate evidence (real tree, 44 projects, 18 LIBRARYs → 28 records):**
  - Double sweep: back-to-back runs, second `appended:0 projects_changed:0`,
    journal + ledger bytes unchanged (shasum-identical).
  - Injected duplicate: scratch copy of Wend LIBRARY (swept repos never
    written), dup line appended → `skipped_duplicate:8 appended:0`, journal
    stays 7 records.
  - Ledger replay: deleted real ledger, re-ran → regenerated byte-identical,
    `appended:0` with all 28 records deduped from the journal (self-heal).
  - Contract cross-check: parser passes all 13 filed line fixtures +
    round-trip file (autonomous/integrations/distillery/contract-tests/).
  - 7 quarantines are real, multi-line-wrapped entries (morphos, edgewise,
    wont) — preserved visibly with raw + provenance; ruling requested in
    distillery-002 / ROADMAP decision 8.
- **Alternatives rejected:** SQLite+jsonl store (human chose jsonl-in-git —
  byte-identical replay is free, SQLite deferrable as a derived index);
  pytest/ruff runner (not installed; stdlib unittest per ecosystem
  convention, ROADMAP decision 4); extending the parser to accept
  multi-line entries (out of contract — filed as a provider ruling instead).
- **Verify:** `./verify full` exit 0 at git d1cc940 (independent verifier
  confirmed: 31 unit tests OK + replay_check OK). Re-run post-commit for the
  new hash. `.harness/last-verify.json` records target full, exit 0.
- **Open questions:** distillery-002 ball is provider (respond-by
  2026-07-18): wrapped-entry ruling, two contract ambiguities (pipes in
  free-text fields, unknown labeled segments), and the Decision-5 tolerance
  readings. Daily sweep schedule wiring is deferred to a post-gate step
  (ROADMAP cadence answer). Neither blocks D1 closure.
